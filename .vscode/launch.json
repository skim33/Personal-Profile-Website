{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Starting the program",
            "program": "${workspaceFolder}/js\\openclose.js"
        }
    ]
}